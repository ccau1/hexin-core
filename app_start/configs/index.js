const FiveBeansConfig = require('./FiveBeansConfig');
const MailerConfig = require('./MailerConfig');
const RedisConfig = require('./RedisConfig');

module.exports = {
  FiveBeansConfig,
  MailerConfig,
  RedisConfig
};
